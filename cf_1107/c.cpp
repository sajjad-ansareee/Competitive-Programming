#include <bits/stdc++.h>

using namespace std;

/*
 * find the maximum possible size palindrome substring
 * if odd length remove the middle part
 * if even length remove the middle_part - 1
 */

void solve() {

}


int main() {
    #ifndef ONLINE_JUDGE
        freopen("input.txt", "r", stdin);
        freopen("output.txt", "w", stdout);
    #endif

    long long t; cin>>t;
    while(t--) {
        solve();
    }
    return 0;
}
